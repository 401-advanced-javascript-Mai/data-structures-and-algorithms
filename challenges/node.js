'use strict';

// built first because it insteation in linkedlist

class Node {
  constructor(value) {
    this.value = value ;
    // when instetion first one it will be the head and the value at sam time
    this.next = null ;
  }
}


module.exports = Node 