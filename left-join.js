
'use strict ' ;

let HashTable = require('./hashtable.js');

function leftJoin (hashmap1 , hashmap2){
//   let hashtable = new HashTable(1024);
  let joinArrayResult = [];
  if (!hashmap1 || !hashmap2){return 'empty hashmap';}

for (let i=0 ; i<hashmap1.length ; i++)
    if (hashmap2.contains[hashmap1[i].[0]])
         joinArrayResult.push([hashmap1[i].[0] ,hashmap1[i].[1] , hashmap2[])   

    /

  };
  _walk(tree1.roo

module.exports = {joinArrayResult} ;
